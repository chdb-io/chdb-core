# The engine version (major.minor) the capability matrix was confirmed against.
# The suite's GetInfo case compares the driver's reported version to this, so a
# baseline bump keeps the suite red until someone reruns it and updates the
# matrix.
SHORT_VERSION = "26.5"
